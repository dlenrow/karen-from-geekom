apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: vlan55-network
  namespace: default
spec:
  config: '{
    "cniVersion": "0.3.1",
    "type": "macvlan",
    "mode": "bridge",
    "master": "enx_w55",
    "ipam": {
      "type": "static",
      "addresses": [
        {
          "address": "192.168.103.1/24"
        }
      ]
    }
  }'

